2026-02-16 13:08:12 | Conectado ao Oracle para ordem 18254562
2026-02-16 13:08:12 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 13:14:13 | Conectado ao Oracle para ordem 18254562
2026-02-16 13:14:13 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 13:16:30 | Conectado ao Oracle para ordem 18254562
2026-02-16 13:16:30 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 13:22:50 | Conectado ao Oracle para ordem 18254562
2026-02-16 13:22:50 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 13:29:31 | Conectado ao Oracle para ordem 18254562
2026-02-16 13:29:31 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 13:29:53 | Conectado ao Oracle para ordem 18254562
2026-02-16 13:29:53 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 14:06:46 | Conectado ao Oracle para ordem 18254562
2026-02-16 14:06:46 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 14:12:38 | Conectado ao Oracle para ordem 18254562
2026-02-16 14:12:38 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
2026-02-16 15:12:58 | Conectado ao Oracle para ordem 18254562
2026-02-16 15:12:58 | SQL: 
        SELECT 
            a.num_ordem as numero_ordem,
            b.seq as sequencia,
            c.descricao as operacao,
            '' as setor,
            'manual' as status_anterior,
            'componentes' as status_novo,
            :colaborador as colaborador,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') || ' 12:00:00' as posicao_fila,
            'Projetos/Dispositivos' as cliente,
            h.cod_item as cod_item,
            a.qtde as qtde,
            TO_CHAR(a.dt_final, 'YYYY-MM-DD') as dt_entrega
        FROM focco3i.tordens a
        INNER JOIN focco3i.tordens_rot b ON (b.ordem_id=a.id)
        INNER JOIN focco3i.toperacao c ON (c.ID=b.OPERACAO_ID)
        CROSS APPLY (
            SELECT d.maquina_id, e.cod_maquina, e.descricao AS desc_maquina
            FROM focco3i.tord_rot_fab_maq d 
            INNER JOIN focco3i.tmaquinas e ON (e.id=d.maquina_id)
            WHERE d.torden_rot_id = b.id 
            ORDER BY d.maquina_id 
            FETCH FIRST 1 ROW ONLY
        ) de
        INNER JOIN focco3i.titens_planejamento g on (g.id=a.itpl_id) 
        INNER JOIN focco3i.titens h on (h.cod_item=g.cod_item) 
        WHERE a.num_ordem = :num_ordem
        ORDER BY b.seq
     | Params: {":num_ordem":"18254562",":colaborador":"Colaborador logado"}
